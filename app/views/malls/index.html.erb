<h1>Malls</h1>
<%= link_to 'Add Mall', new_mall_path %>
<% @malls.each do |mall| %>
<p>
   Name: <%= link_to mall.name, mall_path(mall) %> </br>
   Address: <%= mall.address %> </br> | <%= link_to 'Edit',
   edit_mall_path(mall) %> | <%= link_to 'Delete', mall, method: :delete %>
 </p>
      
    
  <% end %> 
